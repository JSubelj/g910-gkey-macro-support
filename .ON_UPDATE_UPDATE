If you are not me, you can ignore this file.

Raise the version number in:
    - reboot and try
    - pyproject.toml
    - PKGBUILD

Update:
    - Documentation on wiki/readme + changelog
    - PKGBUILD md5 hash

Before release:
    - Check PKG-INFO (generated with python setup.py build) for correct version numbers
    - Check g910-gkeys --version
    - Check pacman for correct version numbers